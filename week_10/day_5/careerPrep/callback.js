// Explain what a 'callback' function is, and provide a simple example.

// A function takes an input, runs commands, and returns an output
// A "callback" function is unique in that it passes a function into another function. 
// An example is an eventListener, for example: a function will not run until a "button" is clicked (i.e. "onClick")