# Given an array A of straings made only from lowercase letters, return a list of all characters that show up in all strings within the list (including duplicates).

# result = []
# A = ["bella", "label", "roller"]

# for item in A:
#     print(A[item])
